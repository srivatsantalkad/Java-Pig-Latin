# Java-Pig-Latin
Pig Latin in Java

Converts any string that the user inputs to the language of Pig Latin, which removes the first character of every word and moves it to the end of corresponding word, then appends a string "ay" to the end of each word.
